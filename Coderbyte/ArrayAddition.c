#include <stdio.h> 
#include <string.h>
#include <stdlib.h>

void ArrayAddition(char* strArr[], int arrLength) {
  
  if(valid){
    printf("%s", "true");
  } else {
    printf("%s", "false");
  }

}

int main(void) { 
   
  // keep this function call here
  //char * A[] = coderbyteInternalStdinFunction(stdin);
  
  int arr1[] = {5,7,16,1,2};
  int arr2[] = {3,5,-1,8,12};

  char* A[] = { matrix[0], matrix[1] };
  int arrLength = sizeof(A) / sizeof(*A);
  //printf("%d\n", A[0][1]);
  TreeConstructor(A, arrLength);
  return 0;
    
}
